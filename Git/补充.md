**下面是pdf速记版本中有以下错误或者需要注意的点**

###### git reset --参数 \<commit-id\>

1. git reset命令用于回退到的之前某一个提交版本，具体版本为对应的commit-id。根据参数不同有以下的区别。
	* --soft：会保留当前工作区和暂存区的所有修改内容。应用场景：多个提交版本可以合并为一个版本的时候就可以通过该命令。
	* --mix：只会保留工作区的内容。应用场景：与--soft相同，只不过再合并时还需要再执行一下git add来将变动的内容重新添加到暂存区。
	* --hard：会将工作区和暂存区都会丢弃。应用场景：放弃目前本地的所有修改内容。比较危险。
2. **查看暂存区内容命令：git ls-files**
3. 命令参数默认为--mixed
4. commit id如果是上一个版本，也可以使用head^来替代，表示上一个历史提交版本
5. **git中所有操作都可以回溯，如果误操作可以使用git reflog查看提交的历史记录（commitid），然后在使用git reset命令回退到当前版本即可**

###### git diff查看差异
![](file-20251225171350250.png)
![](file-20251225173116177.png)
* 不加任何参数默认比较工作区和暂存区之间的差异
* **git diff head表示比较工作区与最近一次提交版本的差异**。
* git diff --cached，比较暂存区和最近一次提交版本的差异
* git diff commit_id commit_id 比较不同提交版本的差异
* git diff commit_id commit_id 1.txt：表示值比较特定文件在不同提交版本的差异


差异内容：  
![](file-20251225171528820.png)
* 第一行：发生变更的文件
* 第二行解释：Git 会将文件的内容使用哈希算法生成一个 40 位的哈希值，这里只显示了部分，后面为文件的权限
* 后面为添加的内容，红色表示删除的内容，绿色表示添加的内容
* 如果不想删除本地，只想删除版本库的中的话，就可以使用git rm --cached，代表只删除暂存区的内容，之后提交即可。

###### git rm file.txt
**普通的linux命令只能将工作区中的内容进行删除，删除暂存区的的内容还需要进行git add；而git rm可以一个命令就可以同时删除工作区和暂存区**

**如果只想删除暂存区的话就使用git rm --cached**


###### .gitignore文件——忽略掉不应该被加入到版本库中的文件
![](file-20251225183455471.png)![](file-20251225184600397.png)

###### git remote
![[file-20260107163909007.png]]

1. **git remote add origin \<url\>**：**将远程仓库地址添加到本质的仓库的配置当中**，这样本地仓库就可以关联远程仓库了。这里origin是给远程仓库起的（别名）名字。

2. **git remote -v**：**查看远程仓库的地址和别名**

3. **git remote rm 别名：删除远程仓库**

4. **git remote rename oldName newName：重命名远程仓库**


###### git pull——拉取远程仓库的最新内容到本地仓库再进行合并
![[file-20260107165855726.png]]
![[file-20260107165142759.png]]
* **若远程分支名和本地分支名一致的话就可以省略成一个名字（git push也一样）**
* **git pull和push命令中的仓库名称和分支名称可以省略，默认pull和push别名为origin的远程仓库的main分支**


###### 分支——可以看作是代码库中的不同版本
![[file-20260108134834565.png]]
* **使用场景：让多个开发人员在自己的分支上进行工作，互不干扰。最后在合并到主线代码库中**
* 分支出现的目的其实就是为了让主线代码仓库处于一个随时可用的稳定的状态，同时还不会影响到其他功能开发和测试，提高团队协作的效率。
* 其基本操作看总结的pdf

操作要点：  
1. **使用 `git branch 新分支名` 创建分支时，新分支会基于你当前所在的分支，并且新分支会完全继承当前分支的所有代码、提交记录。**
2. **当使用git checkout 目标分支名时，当前分支切换到目标分支，工作区会同步为目标分支的代码** 
3. **当使用git checkout -b 新分支名 \[可选：源分支名\]时，他是前两个命令的结合，为创建分支并立即切换到该分支**
4. **注意：git checkout除了用于切换分支外，还用于恢复工作区文件。** 当文件名和分支名相同时就会有歧义。而git checkout会默认切换分支而不是恢复文件。**为避免歧义，git提供了一个新命令专门用于切换分支：git switch 目标分支名**
	![[file-20260108141241940.png]]
	![[file-20260108141458388.png]]
5. **`git merge 目标分支名`，用于将不同分支合并到当前分支中，即代码会自动合并** 所以git merge不仅可以用于将缓存的远程代码合并到当前分支，但该命令已经逐渐被git pull替代，更常用的作用为合并分支。
6. **分支被合并后并不会自动消失，需要手动删除。就需要使用`git branch -参数  目标分支名`来删除分支。其中-d只能删除已经合并的分支；-D代表不管有没有合并，都会删除该分支**  
	![[file-20260108143059645.png]]

###### 解决分支冲突

1. 如果两个分支的内容没有重合的部分的话，git会为我们自动完成合并
2. **但如果两个分支修改同一个文件的同一行代码，git就不知道保存哪个分支的修改内容了，也就产生了冲突**。

解决方案：  
![[file-20260108144441522.png]]
![[file-20260108144508870.png]]
![[file-20260108144531986.png]]
![[file-20260108144554361.png]]


###### git rebase

1. **`git rebase 目标分支名`他是将当前分支合并到目标分支上；而`git merge 目标分支名`是将目标分支合并到当前分支上**  
	![[file-20260108145450732.png]]

二者的优缺点：  
![[file-20260108150128352.png]]![[file-20260108150219045.png]]
* 综上所述最好使用merge合并分支，因为不会破坏原分支的提交历史

###### git分支工作流模型

**工作流模型：一些比较好的规范和流程，可以让我们的工作更规范更高效。**   
![[file-20260108150738395.png]]
![[file-20260108151054127.png]]  
* 主线中的代码不允许直接修改，只能通过合并分支的方式来修改。每次合并分支都建议生成一个新的版本号，可以利用git tag命令来生成版本号。
* 这五个分支中，main分支和develop分支是主要分支，是长期存在。而其他分支在完成各自的功能后就可以删除掉了。


另一种工作流模型——github flow模型   
![[file-20260108151639206.png]]
![[file-20260108152110752.png]]