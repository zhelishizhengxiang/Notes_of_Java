由于之前本人有专门的MybatisPlus的相关笔记，所以次数的笔记较为简略，**只会记录相关的知识点**，没有一些案例等等。详细笔记请见[[Mybatis-plus/README]]

###### MybatisPlus的约定——MP通过扫描实体类，基于反射来获取实体类信息作为进行增删改的数据库表信息，具体的实体类与数据库表信息==默认的对应关系==为：
1. **将类名的驼峰命名转换为用下划线连接来作为表名**。eg：User->user，userInfo->user_info
2. **实体类中名为id的字段来作为表的主键**
3. **将实体类中变量名驼峰转下划线作为表的字段名**。eg:createTime->create_time

如果表名或者是字段名与实体类对应不上，则需要使用下面的常用注解进行标识
# 一、常用注解

### 1.**@TableName

**@TableName用在实体类上，用于标识实体类所对应的表的名字**     
![[file-20260227233147166.png]]

### 2.@TableId

**@TableId用在属性上，用于标识实体类所对应的表主键字段的名字**    
![[file-20260227233307719.png]]
* 图中这种其实是不需要指定也可以，但是如果主键名不是id的话，则需要使用@TableId注解来标识主键名
###### type属性：用于指定id的生成策略
![[file-20260227233836508.png]]
![[file-20260227233825711.png]]
* **如果不指定，MyBatis-Plus在实现插入数据时，会默认基于雪花算法（后面会讲）的策略生成id


### 3.@TableField

**@TableField用在属性上，用于标识实体类所对应的表普通字段的名字**   
![[file-20260227234438542.png]]
**使用@TableField的常见场景**：
1. **成员变量名与数据库字段名不一致**
2. **成员变量名以is开头，且是布尔值**（原因：对于该类型，MP底层在使用反射机制获取字段名称时，会自动将is去掉然后到表中去匹配字段名。eg：isMarried->married来进行匹配）
3. **成员变量名与数据库关键字冲突（还需要对将该变量名使用\'\'括起来将其转义，防止在sql语句中作为关键字来看待）**（上图中order）
4. **成员变量不是数据库字段（exist=false）**

# 二、常用配置

![[file-20260228000520885.png]]
* **上图中的所有配置项都是MP的默认值**，除了包扫描路径
* 其中glabal-config上面的配置都是mybatis的配置，MP在这里都同样拥有这些配置（本身MP就是对Mybatis的补充），只不过配置项前缀变为了mabatis-plus。
* **glabal-config是全局有效。但是配置项的配置优先级没有使用注解配置的优先级高**。所以只要使用注解进行配置，就会覆盖全局配置


# 三、条件构造器（Wrapper）
![[file-20260228001534268.png]]

1. **Wrapper用于构造sql语句中的复杂where条件。具体是用Wrapper提供的方法来构造，每种条件都有对应的方法**  
	![[file-20260228001559632.png]]

2. **QueryWrapper在AbstractWrapper的基础上，又扩展了可以筛选select字段的功能。**（写入查询字段的内容，底层会自动做拼接）         
	![[file-20260228001724475.png]]

3. **UpdateWrapper在AbstractWrapper的基础上，又扩展了可以设置set的字段的功能** （写入设置字段的内容，底层会自动做拼接）        
	![[file-20260228001921356.png]]

Lambda\*与上述说的Wrapper功能一样，只是在构造条件时基于的Lambda语法来进行构建

**使用总结**：  
![[file-20260228004741434.png]]
###### 使用举例1：查询出名字中带o的，存款大于等于1000元的人的id、username、info、balance字段  
![[file-20260228002216878.png]]
![[file-20260228003150004.png]]
**实际应用推荐使用Lambda形式来防止硬编码**，具体如下图。
![[file-20260228004506634.png]]

###### 使用举例2：更新用户名为jack的用户的余额为2000
![[file-20260228002308923.png]]
![[file-20260228003406746.png]]




###### 使用举例3：更新id为1，2，4的用户的余额，为原先减掉200
![[file-20260228003601896.png]]
![[file-20260228004024370.png]]

# 四、自定义sql
对于比较复杂的sql语句，例如要对select字段进行聚合函数的运算等等，使用MP来进行拼接这样属于是硬编码，不遵循企业规范，不推荐使用。

**所以对于这样一些比较复杂的sql语句，我们可以们可以利用MyBatisPlus的Wrapper来构建复杂的Where条件，然后自己定义SQL语句中剩下的部分**。这样可以起到简化sql语句编写的作用

###### 运用实例：将id在指定范围的用户（例如1、2、4 ）的余额扣减指定值
![[file-20260228174133228.png]]
* **在mapper方法参数中用Param注解声明wrapper变量名称，必须是ew**
* **使用`${ew.customSqlSegment}`来拼接where条件**，将来MP会自动对where条件进行拼接


# 五、IService接口
MP不仅提供通用的Mapper，还提供通用的service接口。

* 提供的service接口就是**IService**。该接口进一步封装 CRUD 采用 **get** 查询单行 **remove** 删除 **list** 查询集合 **page** 分页 **save** 插入，前缀命名方式区分 Mapper 层避免混淆。**泛型 T 为操作的实体类对象**    
	![[file-20260228175654487.png]]
* 建议如果存在自定义通用 Service 方法的可能，请创建自己的 IBaseService 继承
* **该接口还有一个实现类`ServiceImpl<M extends BaseMapper<T>, T>`，封装了常见的业务层逻辑。其泛型一共有两个。第一个为我们所写的Mapper，第二个为我们所要操作的实体类对象**

### 1.使用方法
实际场景中较为复杂，ServiceImpl无法满足需求，所以不建议直接使用ServiceImpl。
 
 **建议独自创建Service接口来继承Iservice，独自创建实现类来继承ServiceImpl再继承自己写的Service**。这与既可以使用Iservice提供的功能，也可以使用自定义的功能。  
```java
// 接口  UserService继承IService模板提供的基础功能
public interface UserService extends IService<User> {

}
// 实现类
/*
   ServiceImpl实现了IService，提供了IService中基础功能的实现
   若ServiceImpl无法满足业务需求，则可以使用自定的UserService定义方法，
   并在实现类中实现
*/
@Service
public class UserServiceImpl extends ServiceImpl<UserMapper, User> implements UserService {

}
```

### 2.使用举例
 1. 测试记录总数
![[file-20260109211214277.png]]
![[file-20260109211158611.png]]


2. 测试批量添加功能   
![[file-20260109211443296.png]]
![[file-20260109211524400.png]]


完整代码如下图所示：  
![[file-20260228181452009.png]]

### 3.Iservice接口中Lambda方法（用于解决动态sql的问题）

###### lambdaQuery()
需求：实现一个根据复杂条件查询用户的接口，查询条件如下：
* name：用户名关键字，可以为空
* status：用户状态，可以为空
* minBalance：最小余额，可以为空
* maxBalance：最大余额，可以为空

controller    
![[file-20260228231534417.png]]

service  
![[file-20260228231933820.png]]
* lambdaUpdate()与其同理此处就不再演示，只给出代码截图     
	![[file-20260228232639883.png]]
	
	

# 六、扩展功能

此处只讲解本人认为有用的扩展功能，并不包括视频中讲解的所有扩展功能
### 1.代码生成

代码生成器：根据表信息自动生成如下图所示的相关内容  
![[file-20260301000755943.png]]

不使用官方文档的推荐的内容，而是推荐使用以下插件来做代码生成  
![[file-20260301001017775.png]]

使用步骤  
![[file-20260301001050081.png]]
![[file-20260301001058377.png]]![[file-20260301001106950.png]]
![[file-20260301001112046.png]]


### 2.逻辑删除
逻辑删除就是基于代码逻辑模拟删除效果，但并不会真正删除数据。思路如下：
* **在表中添加一个字段标记数据是否被删除**
* **当删除数据时把标记置为1**
* **查询时只查询标记为0的数据**

此时逻辑删除一个记录的操作就变成了update操作，查询操作就只查询的是有效数据  
![[file-20260301002806299.png]]

那么此时引入逻辑删除字段后对应的增删改查的语句都发生了变化，那么之前MP默认提供的哪些api底层的语句都不符合要求了。那么对此的具体解决办法如下：  

**MybatisPlus提供了逻辑删除功能，无需改变方法调用的方式，而是在底层帮我们自动修改CRUD的语句。我们要做的就是在application.yaml文件中配置逻辑删除的字段名称和值即可**，具体配置如下

```yml
mybatis-plus: 
  global-config: 
    db-config: 
    logic-delete-field: flag # 全局逻辑删除的实体字段名, 字段类型可以是boolean、integer 
    logic-delete-value: 1 # 逻辑已删除值(默认为 1) 
    logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)
```

逻辑删除本身也有自己的问题，比如：
* 会导致数据库表垃圾数据越来越多，影响查询效率
* SQL中全都需要对逻辑删除字段做判断，影响查询效率   
因此，不太推荐采用逻辑删除功能，如果数据不能删除，可以采用把数据迁移到其它表的办法。

### 3.分页插件
在只使用Mybatis的时候，通常使用PageHelper来实现分页，当使用了MP之后，其内部就包含分页功能，就可以不用单独引入PageHelper依赖来进行操作。
###### 基本用法
![[file-20260301004839241.png]]
![[file-20260301005147601.png]]
![[file-20260301005417503.png]]
* 此处的Page其实和PageHelper中的配置差不多的原理（可以参考[[02常用技术点]]）
###### 通用分页的封装使用

1. 当接收分页接收查询参数时，一般分页查询与业务无关，什么实体都会有分页查询，所以**定义一个类专门为PageQuery来接收分页参数，当某个实体类需要分页查询时直接继承该类即可。其次将查询参数PageQuery转换为MP需要的Page这个步骤时每次查询都会有的，所以我们将其封装为一个方法放在顶层的PageQuery方法中**  
	![[file-20260312205001944.png]]
	![[file-20260312204544287.png]]


2. **同理，从Page对象中封装出查询出来的分页信息，这个也是通用逻辑，所以同样需要封装成一个方法来进行调用**。  
	![[file-20260312210422133.png]]


使用方式：  
![[file-20260312210509664.png]]